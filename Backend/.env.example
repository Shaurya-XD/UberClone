PORT=4000
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<database>
JWT_SECRET=replace-with-a-long-random-secret
# Comma-separated origins allowed to access a separately hosted API.
CORS_ORIGIN=http://localhost:5173,http://localhost:4000,https://your-domain.example
